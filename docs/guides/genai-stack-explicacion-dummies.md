# GenAI Stack - Explicación para Dummies-Redummies

**Fecha**: 2026-01-10
**Audiencia**: Personas que NO entienden Docker ni contenedores

---

## 🤔 TU PREGUNTA: "¿No es GenAI Stack un Docker container?"

### ✅ RESPUESTA CORTA: SÍ, ES 100% DOCKER

GenAI Stack es un **conjunto de containers Docker** que incluye **TODO**:
- Neo4j (base de datos)
- Ollama (LLMs locales) ← **AQUÍ ESTÁ LA CONFUSIÓN**
- LangChain (framework)
- 5 aplicaciones de ejemplo

**NO necesitas instalar NADA más** (excepto Docker Desktop)

---

## 😵 ¿POR QUÉ LA CONFUSIÓN?

### Problema 1: Dos perfiles de Ollama

GenAI Stack tiene **2 formas de usar Ollama**:

```
OPCIÓN A: Ollama DENTRO de Docker (Linux only)
┌────────────────────────────────────────┐
│  Docker Desktop                        │
│  ┌──────────────────────────────────┐  │
│  │  Container: Ollama               │  │
│  │  - qwen2.5:latest                │  │
│  │  - nomic-embed-text              │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │  Container: Neo4j                │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │  Container: Bot (app)            │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

OPCIÓN B: Ollama FUERA de Docker (Windows/Mac) ← TÚ USAS ESTA
┌────────────────────────────────────────┐
│  Tu Windows                            │
│  ┌──────────────────────────────────┐  │
│  │  Ollama.exe (corriendo)          │  │ ← Instalado aparte
│  │  - qwen2.5:latest                │  │
│  │  - nomic-embed-text              │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
           ↓ ↑ (HTTP)
┌────────────────────────────────────────┐
│  Docker Desktop                        │
│  ┌──────────────────────────────────┐  │
│  │  Container: Neo4j                │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │  Container: Bot (app)            │  │
│  │  (conecta a Ollama FUERA)        │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### ¿Por qué existe OPCIÓN B?

**Razón**: En Windows/Mac, Ollama dentro de Docker es **LENTO** (sin GPU)

**Solución GenAI Stack**: Usa Ollama instalado en tu Windows (más rápido con GPU)

---

## 🎯 DIAGRAMA COMPLETO: ¿Qué es GenAI Stack?

### Arquitectura Física

```
════════════════════════════════════════════════════════════════
                    TU COMPUTADORA (Windows)
════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────┐
│  FUERA DE DOCKER                                           │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Ollama.exe (C:\Users\...\Ollama\)                   │ │
│  │  • Puerto: 11434                                     │ │
│  │  • Modelos descargados:                              │ │
│  │    - qwen2.5:latest (7B, para texto)                │ │
│  │    - nomic-embed-text (para embeddings)             │ │
│  └──────────────────────────────────────────────────────┘ │
│                          ↕ HTTP                           │
└────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════
              DOCKER DESKTOP (Máquina Virtual)
════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 1: Neo4j                                       │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Imagen: neo4j:5.26                                  │ │
│  │  Puertos:                                            │ │
│  │    • 7474 → Neo4j Browser (UI web)                  │ │
│  │    • 7687 → Bolt (conexión programática)            │ │
│  │  Datos: C:\proyectos\..\_lab\genai-stack\data\     │ │
│  │  Incluye:                                            │ │
│  │    • Base de datos graph                            │ │
│  │    • Vector index (HNSW)                            │ │
│  │    • APOC plugins                                   │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 2: pull-model                                  │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Propósito: Descargar modelos a Ollama              │ │
│  │  Script: pull_model.Dockerfile                       │ │
│  │  Acción:                                             │ │
│  │    1. Conecta a Ollama (fuera de Docker)            │ │
│  │    2. Ejecuta: ollama pull qwen2.5:latest           │ │
│  │    3. Ejecuta: ollama pull nomic-embed-text         │ │
│  │    4. Se detiene (restart: no)                      │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 3: bot (Chatbot de Soporte)                   │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Framework: Streamlit + LangChain                    │ │
│  │  Puerto: 8501                                        │ │
│  │  Conecta a:                                          │ │
│  │    • Neo4j (bolt://database:7687)                   │ │
│  │    • Ollama (http://host.docker.internal:11434)     │ │
│  │  Código: bot.py                                      │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 4: loader (Cargador de Datos)                 │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Propósito: Ingestar datos StackOverflow            │ │
│  │  Puerto: 8502                                        │ │
│  │  Acción:                                             │ │
│  │    1. Descarga Q&A de StackOverflow API             │ │
│  │    2. Genera embeddings (via Ollama)                │ │
│  │    3. Almacena en Neo4j (nodos + vectores)          │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 5: pdf_bot (Q&A sobre PDFs)                   │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Framework: Streamlit + LangChain                    │ │
│  │  Puerto: 8503                                        │ │
│  │  Función: Subir PDF → Hacer preguntas → RAG         │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 6: api (REST API)                             │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Framework: FastAPI                                  │ │
│  │  Puerto: 8504                                        │ │
│  │  Endpoints: /query, /ingest, /health                │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  CONTENEDOR 7: front-end (UI Svelte)                      │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Framework: Svelte + Vite                            │ │
│  │  Puerto: 8505                                        │ │
│  │  Conecta a: API (8504)                               │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════
```

---

## 🔄 DIAGRAMA DE FLUJO: ¿Cómo Funciona?

### Flujo 1: Inicialización (docker-compose up)

```
┌─────────────────────────────────────────────────────────────┐
│  PASO 1: Usuario ejecuta comando                           │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
              cd _lab\genai-stack
              docker-compose up -d
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  PASO 2: Docker Compose lee docker-compose.yml             │
│  • Detecta 7 servicios definidos                           │
│  • Lee .env (variables de entorno)                         │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  PASO 3: Inicia servicios en ORDEN (depends_on)            │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3.1: database (Neo4j)                             │    │
│  │       • Crea container                             │    │
│  │       • Espera healthcheck (wget localhost:7474)   │    │
│  │       • Status: HEALTHY (30 segundos)              │    │
│  └────────────────────────────────────────────────────┘    │
│                         │                                   │
│                         ▼                                   │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3.2: pull-model                                   │    │
│  │       • Espera: database HEALTHY                   │    │
│  │       • Ejecuta: ollama pull qwen2.5:latest        │    │
│  │       • Ejecuta: ollama pull nomic-embed-text      │    │
│  │       • Status: COMPLETED (5-10 min)               │    │
│  │       • Se detiene (restart: no)                   │    │
│  └────────────────────────────────────────────────────┘    │
│                         │                                   │
│                         ▼                                   │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3.3: bot, loader, pdf_bot (en paralelo)           │    │
│  │       • Esperan: pull-model COMPLETED              │    │
│  │       • Inician apps Streamlit                     │    │
│  │       • Status: RUNNING                            │    │
│  └────────────────────────────────────────────────────┘    │
│                         │                                   │
│                         ▼                                   │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3.4: api                                           │    │
│  │       • Espera: database HEALTHY                   │    │
│  │       • Inicia FastAPI                             │    │
│  │       • Healthcheck: wget localhost:8504           │    │
│  └────────────────────────────────────────────────────┘    │
│                         │                                   │
│                         ▼                                   │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3.5: front-end                                     │    │
│  │       • Espera: api HEALTHY                        │    │
│  │       • Inicia Svelte dev server                   │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  PASO 4: Stack completo RUNNING                            │
│  • Neo4j Browser: http://localhost:7474                    │
│  • Chatbot: http://localhost:8501                          │
│  • Loader: http://localhost:8502                           │
│  • PDF Bot: http://localhost:8503                          │
│  • API: http://localhost:8504                              │
│  • Frontend: http://localhost:8505                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 DIAGRAMA DE SECUENCIA: RAG Query (Pregunta al Chatbot)

### Ejemplo: Usuario pregunta "¿Cómo usar Neo4j?"

```
Usuario          Bot (8501)         Ollama (11434)      Neo4j (7687)
  │                 │                    │                  │
  │  1. Pregunta    │                    │                  │
  │ "¿Cómo usar     │                    │                  │
  │  Neo4j?"        │                    │                  │
  ├────────────────>│                    │                  │
  │                 │                    │                  │
  │                 │  2. Embed query    │                  │
  │                 │  (convertir a      │                  │
  │                 │   vector 768-dim)  │                  │
  │                 ├───────────────────>│                  │
  │                 │                    │                  │
  │                 │  3. Embedding      │                  │
  │                 │  [0.12, -0.34,..] │                  │
  │                 │<───────────────────┤                  │
  │                 │                    │                  │
  │                 │  4. Vector Search                     │
  │                 │  (CALL db.index.vector.queryNodes)    │
  │                 ├──────────────────────────────────────>│
  │                 │                    │                  │
  │                 │                    │  5. HNSW Search  │
  │                 │                    │  (logarítmico)   │
  │                 │                    │  • Busca similar │
  │                 │                    │  • Top-5 chunks  │
  │                 │                    │                  │
  │                 │  6. Resultados                        │
  │                 │  [{chunk, score}, ...]                │
  │                 │<──────────────────────────────────────┤
  │                 │                    │                  │
  │                 │  7. Graph Traversal                   │
  │                 │  (MATCH (chunk)-[:BELONGS_TO]->(q))   │
  │                 ├──────────────────────────────────────>│
  │                 │                    │                  │
  │                 │  8. Metadata                          │
  │                 │  (preguntas + respuestas completas)   │
  │                 │<──────────────────────────────────────┤
  │                 │                    │                  │
  │                 │  9. Construir Prompt                  │
  │                 │  "Contexto: [chunks]                  │
  │                 │   Pregunta: ¿Cómo usar Neo4j?         │
  │                 │   Responde usando contexto"           │
  │                 │                    │                  │
  │                 │  10. Generate                         │
  │                 │  (LLM genera texto)                   │
  │                 ├───────────────────>│                  │
  │                 │                    │                  │
  │                 │                    │  11. Procesa     │
  │                 │                    │  prompt con      │
  │                 │                    │  qwen2.5:latest  │
  │                 │                    │                  │
  │                 │  12. Respuesta                        │
  │                 │  "Para usar Neo4j...                  │
  │                 │   [respuesta generada]"               │
  │                 │<───────────────────┤                  │
  │                 │                    │                  │
  │  13. Display    │                    │                  │
  │  respuesta +    │                    │                  │
  │  fuentes        │                    │                  │
  │<────────────────┤                    │                  │
  │                 │                    │                  │
  │  "Para usar     │                    │                  │
  │  Neo4j puedes...│                    │                  │
  │                 │                    │                  │
  │  Fuentes:       │                    │                  │
  │  • stackoverflow│                    │                  │
  │    .com/q/12345"│                    │                  │
```

**Latencias típicas**:
- Embed query (paso 2-3): ~100ms
- Vector search (paso 4-6): ~50ms (HNSW)
- Graph traversal (paso 7-8): ~30ms
- LLM generation (paso 10-12): ~2-5 segundos (depende del modelo)
- **TOTAL**: ~3-6 segundos

---

## 🔍 DIAGRAMA DE USO: Escenarios Completos

### Escenario 1: Primera Vez (Setup Completo)

```
TIEMPO: 15 minutos

┌─────────────────────────────────────────────────────────────┐
│  1. Instalar Docker Desktop                                 │
│     • Descargar de docker.com                               │
│     • Instalar con defaults                                 │
│     • Reiniciar computadora                                 │
│     ⏱️ 10 minutos                                            │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Instalar Ollama (Windows)                               │
│     • Descargar de ollama.ai/download                       │
│     • Ejecutar instalador                                   │
│     • Verificar: ollama list                                │
│     ⏱️ 2 minutos                                             │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Clonar GenAI Stack                                      │
│     cd C:\proyectos\aleia-melquisedec\_lab                  │
│     git clone https://github.com/docker/genai-stack.git     │
│     cd genai-stack                                          │
│     ⏱️ 1 minuto                                              │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Configurar .env                                         │
│     copy .env.example .env                                  │
│     • Editar: LLM=qwen2.5:latest                            │
│     • Editar: EMBEDDING_MODEL=nomic-embed-text              │
│     • Editar: OLLAMA_BASE_URL=http://host.docker.int...     │
│     ⏱️ 2 minutos                                             │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  5. Iniciar Stack                                           │
│     docker-compose up -d                                    │
│     • Descarga imágenes (primera vez: 2GB)                  │
│     • Inicia containers                                     │
│     • Descarga modelos Ollama (qwen2.5: 4.7GB)             │
│     ⏱️ 10-15 minutos (depende de internet)                  │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  6. Verificar                                               │
│     docker ps                                               │
│     • Debe mostrar 7 containers (o 6 si pull-model terminó)│
│     • Neo4j Browser: http://localhost:7474                  │
│     • Credenciales: neo4j / password                        │
│     ⏱️ 1 minuto                                              │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  ✅ LISTO PARA USAR                                         │
└─────────────────────────────────────────────────────────────┘
```

---

### Escenario 2: Uso Diario (Ya Configurado)

```
TIEMPO: 2 minutos

┌─────────────────────────────────────────────────────────────┐
│  1. Iniciar Docker Desktop                                  │
│     • Click en icono Docker                                 │
│     • Esperar "Docker is running"                           │
│     ⏱️ 30 segundos                                           │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Verificar Ollama corriendo                              │
│     • Buscar icono Ollama en system tray                    │
│     • Si no está: Ejecutar Ollama desde menú inicio         │
│     ⏱️ 10 segundos                                           │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Iniciar GenAI Stack                                     │
│     cd C:\proyectos\aleia-melquisedec\_lab\genai-stack      │
│     docker-compose up -d                                    │
│     ⏱️ 30 segundos (containers ya existen)                  │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Abrir app                                               │
│     • Chatbot: http://localhost:8501                        │
│     • PDF Bot: http://localhost:8503                        │
│     • Neo4j Browser: http://localhost:7474                  │
│     ⏱️ 10 segundos                                           │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  ✅ TRABAJANDO                                              │
└─────────────────────────────────────────────────────────────┘

CUANDO TERMINES:

┌─────────────────────────────────────────────────────────────┐
│  5. Detener (mantiene datos)                                │
│     docker-compose down                                     │
│     ⏱️ 10 segundos                                           │
└─────────────────────────────────────────────────────────────┘

o

┌─────────────────────────────────────────────────────────────┐
│  6. Detener y borrar datos                                  │
│     docker-compose down -v                                  │
│     ⏱️ 10 segundos                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 EJEMPLO COMPLETO END-TO-END

### Caso: Hacer una pregunta sobre Python al chatbot

```
════════════════════════════════════════════════════════════════
PASO 1: Iniciar Stack
════════════════════════════════════════════════════════════════

PowerShell:
  cd C:\proyectos\aleia-melquisedec\_lab\genai-stack
  docker-compose up -d

Output:
  [+] Running 7/7
   ✔ Container genai-stack-database-1    Started  (1.2s)
   ✔ Container genai-stack-pull-model-1  Started  (2.4s)
   ✔ Container genai-stack-bot-1         Started  (3.1s)
   ✔ Container genai-stack-loader-1      Started  (3.2s)
   ✔ Container genai-stack-pdf_bot-1     Started  (3.3s)
   ✔ Container genai-stack-api-1         Started  (4.5s)
   ✔ Container genai-stack-front-end-1   Started  (5.2s)

════════════════════════════════════════════════════════════════
PASO 2: Verificar Containers
════════════════════════════════════════════════════════════════

PowerShell:
  docker ps

Output:
  CONTAINER ID   IMAGE                  STATUS                   PORTS
  abc123def456   genai-stack-bot        Up 1 minute (healthy)    0.0.0.0:8501->8501/tcp
  def456ghi789   neo4j:5.26             Up 1 minute (healthy)    0.0.0.0:7474->7474/tcp, 0.0.0.0:7687->7687/tcp
  ...

════════════════════════════════════════════════════════════════
PASO 3: Cargar Datos StackOverflow
════════════════════════════════════════════════════════════════

Navegador:
  http://localhost:8502 (Loader UI)

Acción:
  1. Seleccionar tags: "python", "pandas"
  2. Click "Load Data"
  3. Esperar: "✅ Loaded 500 questions"

Proceso interno (ver diagrama anterior):
  • Loader descarga Q&A de StackOverflow API
  • Genera embeddings para cada pregunta (Ollama)
  • Almacena en Neo4j:
    - Nodos: Question, Answer, User, Tag
    - Relaciones: [:ANSWERS], [:ASKED], [:TAGGED]
    - Embeddings: question.embedding (array 768-dim)

════════════════════════════════════════════════════════════════
PASO 4: Verificar Datos en Neo4j Browser
════════════════════════════════════════════════════════════════

Navegador:
  http://localhost:7474

Login:
  Usuario: neo4j
  Contraseña: password

Cypher Query:
  // Contar preguntas
  MATCH (q:Question) RETURN count(q) AS total;

Output:
  total
  ─────
  500

Cypher Query:
  // Ver una pregunta con embeddings
  MATCH (q:Question)
  WHERE q.embedding IS NOT NULL
  RETURN q.title, size(q.embedding) AS embedding_dim
  LIMIT 1;

Output:
  q.title                              embedding_dim
  ──────────────────────────────────  ─────────────
  "How to read CSV in pandas?"        768

Cypher Query:
  // Ver relaciones
  CALL db.schema.visualization();

Output (gráfico):
  (Question)-[:ANSWERS]->(Answer)
  (User)-[:ASKED]->(Question)
  (Question)-[:TAGGED]->(Tag)

════════════════════════════════════════════════════════════════
PASO 5: Hacer Pregunta al Chatbot
════════════════════════════════════════════════════════════════

Navegador:
  http://localhost:8501 (Chatbot UI)

UI:
  ┌─────────────────────────────────────────────────────────┐
  │  GenAI Stack - Support Bot                             │
  ├─────────────────────────────────────────────────────────┤
  │                                                         │
  │  💬 Chat                                                │
  │                                                         │
  │  [Input box] ¿Cómo leer un CSV en pandas?              │
  │                                               [Send]    │
  └─────────────────────────────────────────────────────────┘

Acción:
  1. Escribir: "¿Cómo leer un CSV en pandas?"
  2. Click "Send"
  3. Ver respuesta (tarda 3-5 segundos)

════════════════════════════════════════════════════════════════
PASO 6: Proceso Interno (RAG Pipeline)
════════════════════════════════════════════════════════════════

Ver "DIAGRAMA DE SECUENCIA" arriba para detalles completos.

Resumen:
  1. Bot → Ollama: Embed query "¿Cómo leer CSV pandas?"
  2. Bot → Neo4j: Vector search (HNSW index)
  3. Neo4j → Bot: Top-5 preguntas similares
  4. Bot → Neo4j: Graph traversal (obtener respuestas completas)
  5. Bot → Ollama: Generate response con contexto
  6. Ollama → Bot: Respuesta generada
  7. Bot → Usuario: Display con fuentes

════════════════════════════════════════════════════════════════
PASO 7: Respuesta del Chatbot
════════════════════════════════════════════════════════════════

UI:
  ┌─────────────────────────────────────────────────────────┐
  │  🤖 Bot                                                  │
  │                                                         │
  │  Para leer un archivo CSV en pandas, usa:              │
  │                                                         │
  │  ```python                                              │
  │  import pandas as pd                                    │
  │                                                         │
  │  df = pd.read_csv('archivo.csv')                        │
  │  print(df.head())  # Ver primeras filas                │
  │  ```                                                    │
  │                                                         │
  │  Opciones comunes:                                      │
  │  • `sep=';'` para otro separador                        │
  │  • `encoding='utf-8'` para caracteres especiales        │
  │  • `index_col=0` para usar primera columna como índice │
  │                                                         │
  │  📚 Fuentes:                                            │
  │  • stackoverflow.com/questions/123456                   │
  │  • stackoverflow.com/questions/789012                   │
  └─────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════
PASO 8: Verificar Vector Search en Neo4j
════════════════════════════════════════════════════════════════

Navegador:
  http://localhost:7474

Cypher Query (simulando lo que hizo el bot):
  // 1. Simular embedding del query (en realidad es un array de 768 números)
  WITH [0.12, -0.34, 0.56, ...] AS queryEmbedding

  // 2. Vector search
  CALL db.index.vector.queryNodes(
    'stackoverflow',  // Index name
    5,                // top_k
    queryEmbedding
  )
  YIELD node, score
  RETURN node.title AS question, score
  ORDER BY score DESC;

Output:
  question                                    score
  ─────────────────────────────────────────  ─────
  "How to read CSV file in pandas?"          0.92
  "pandas read_csv encoding issues"          0.87
  "Read CSV with custom separator pandas"    0.84
  "Import data from CSV pandas DataFrame"    0.81
  "Parse CSV file Python pandas"             0.78

════════════════════════════════════════════════════════════════
✅ EJEMPLO COMPLETO
════════════════════════════════════════════════════════════════

Has visto:
  ✅ Cómo iniciar GenAI Stack (docker-compose up -d)
  ✅ Cómo cargar datos (Loader UI)
  ✅ Cómo verificar en Neo4j (Cypher queries)
  ✅ Cómo hacer preguntas (Chatbot UI)
  ✅ Proceso interno completo (RAG pipeline con diagramas)
```

---

## 🆚 COMPARACIÓN FINAL: ¿Qué Incluye GenAI Stack?

```
┌─────────────────────────────────────────────────────────────┐
│  ✅ INCLUIDO EN GENAI STACK (Containers Docker)             │
├─────────────────────────────────────────────────────────────┤
│  • Neo4j 5.26 (base de datos graph+vector)                 │
│  • LangChain (framework RAG)                                │
│  • 5 aplicaciones de ejemplo (bot, loader, pdf_bot, etc.)  │
│  • Scripts de ingesta (loader.py)                           │
│  • Configuración Docker Compose                             │
│  • Healthchecks automáticos                                 │
│  • Volume persistence (datos no se pierden)                 │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  ⚠️  NO INCLUIDO O REQUIERE INSTALACIÓN EXTERNA             │
├─────────────────────────────────────────────────────────────┤
│  • Docker Desktop (REQUERIDO - sin esto no funciona nada)  │
│                                                             │
│  • Ollama (OPCIONES):                                       │
│    OPCIÓN A (TODO EN DOCKER):                               │
│      → docker compose --profile linux up                   │
│      → Funciona en Linux, Windows (WSL2), Mac (VM)         │
│      → .env: OLLAMA_BASE_URL=http://llm:11434              │
│      → ✅ TODO incluido, sin instalación externa           │
│                                                             │
│    OPCIÓN B (OLLAMA NATIVO - MEJOR RENDIMIENTO GPU):       │
│      → Instalar Ollama nativo en Windows/Mac               │
│      → docker compose up (sin --profile)                   │
│      → .env: OLLAMA_BASE_URL=http://host.docker.internal   │
│      → ⚡ Mejor rendimiento con GPU local                  │
│                                                             │
│  • Modelos Ollama (qwen2.5, nomic-embed-text):             │
│    → Se descargan automáticamente con pull-model container │
│    → PERO es más rápido descargarlos antes manualmente     │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 RESUMEN PARA DUMMIES-REDUMMIES

### ¿GenAI Stack es un container?
SÍ**, pero tienes 2 opciones:

**OPCIÓN A (TODO EN DOCKER)**:
- `docker compose --profile linux up`
- Funciona en **cualquier plataforma** (Linux, Windows con WSL2, Mac con VM)
- Ollama corre en contenedor `ollama/ollama:latest`
- `.env`: `OLLAMA_BASE_URL=http://llm:11434`

**OPCIÓN B (OLLAMA NATIVO - MEJOR RENDIMIENTO)**:
- Instalar Ollama localmente en Windows/Mac
- `docker compose up` (sin `--profile`)
- Mejor acceso a GPU local
- `.env`: `OLLAMA_BASE_URL=http://host.docker.internal:11434`

**Mi error anterior**: Dije que Ollama NO está incluido en Windows/Mac, pero el perfil Linux SÍ funciona en Windows/Mac gracias a Docker Desktop (usa WSL2 en Windows y VM en Mac).
1. **Neo4j** (base de datos)
2. **pull-model** (descarga modelos Ollama, luego se detiene)
3. **bot** (chatbot con Streamlit)
4. **loader** (carga datos StackOverflow)
5. **pdf_bot** (Q&A sobre PDFs)
6. **api** (REST API con FastAPI)
7. **front-end** (UI con Svelte)

### ¿Ollama está incluido?

**DEPENDE*Elige una opción:

**OPCIÓN A (TODO EN DOCKER - RECOMENDADO PARA EMPEZAR)**:
```powershell
cd _lab\genai-stack
docker compose --profile linux up -d
```

**OPCIÓN B (OLLAMA NATIVO - MEJOR RENDIMIENTO)**:
```powershell
# Terminal 1: Inicia Ollama
ollama serve

# Terminal 2: Inicia stack
cd _lab\genai-stack
docker compose up -d
```

Espera 1-2 minutos (descarga modelos)(obligatorio)
2. Ollama (recomendado en Windows/Mac)
3. GenAI Stack: `git clone` (ya lo tienes en `_lab/genai-stack/`)

### ¿Listo para usar?

**Casi**. Solo ejecuta:
```powershell
cd _lab\genai-stack
docker-compose up -d
```

Espera 1-2 minutos, luego abre http://localhost:8501

---

**¿Más dudas?** Revisa los diagramas arriba o pregunta específicamente qué parte no entiendes.
